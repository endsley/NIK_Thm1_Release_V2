
To ensure you have all the package installed, run

	pip install -r requirements.txt

To run the code on a linux machine

	./thm1_simulation.py


This runs the adversairal dataset. If you want to run a different
dataset, open up thm1_simulation.py to comment out adversarial and 
uncomment the other data.
